[project]
name = "business-cycle-backtester"
version = "0.1.0"
description = "Business cycle and macro regime backtesting toolkit."
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "openpyxl>=3.1",
    "pandas>=2.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
